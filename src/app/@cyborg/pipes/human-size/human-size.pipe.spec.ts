import { HumanSizePipe } from './human-size.pipe';

describe('HumanSizePipe', () => {
  it('create an instance', () => {
    const pipe = new HumanSizePipe();
    expect(pipe).toBeTruthy();
  });
});
